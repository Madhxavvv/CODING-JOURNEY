#include <stdio.h>
int main(){
    int a = 9;
    int b = 2;
    float c = (float)b; //type casting means changing the data type of a variable
    printf("The value of c is %f", c);

    return 0;
}