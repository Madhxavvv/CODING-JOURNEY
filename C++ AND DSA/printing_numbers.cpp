#include <iostream>
using namespace std;
int main(){
    cout<<18+1<<endl; // if numbers are written as it is without double quotes then they are treated as numbers
    cout<<"18+1"<<endl; // anything written inside double quotes they are treated as characters
    return 0;
}