#include <stdio.h>
int main()
{
    int a = 3, b = 2, c = 1;
    (a > b) ? printf("A is greater") : printf("B is greater");
    return 0;
}

// condition1 ? statement if true : statement if false
