#include <stdio.h>
int main(){
    char* ptr = "MADHAV";
    puts(ptr);
    ptr = "HARRY";  
    puts(ptr);

    return 0;
}

// string can be initialized using pointer if it needs to change thoroughly during the program
